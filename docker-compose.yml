version: '3.8'

services:
  frontend:
    build:
      context: ./ims-admin
      dockerfile: Dockerfile
    image: ims-admin:latest
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://host.docker.internal:5079